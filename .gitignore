# Model weights (too large for GitHub — bundled in .app instead)
ner/*.pt
ner/*.pkl
ner/joint_entity_role_model/
ner/provenance_model_kfold/
ner/contents_model_kfold/

# Training-data caches (large; regenerated from source TSVs)
data/tsvs/genre_samples.tsv
data/tsvs/marc500_sentences.tsv
data/tsvs/filtered_manuscripts_after_906a.tsv

# Paper draft directory (entire dir excluded — not for public repo).
# Includes .tex sources, figures, verification scripts, literature,
# build artifacts, and all subdirectories.
paper/

# Reference PDFs (large)
docs/*.pdf

# Sensitive / external
docs/mails from experts/

# Coding-agent transient state
.codex/
.github/ai/
.claude/scheduled_tasks.lock

# Build artifacts
dist/
*.dmg

# Python bytecache
__pycache__/
*.pyc

# Logs
*.log
.coverage

# macOS
.DS_Store

# Data files (large) — NLI authority + KIMA third-party + MARC dumps
data/kima/kima_index.db
data/kima/*.tsv
data/NLI_AUTHORITY_XML/
data/marc-subset-10k.*
data/catalog-v001.xml
data/tsvs/17th_century_samples.tsv
data/tsvs/top100_richest.tsv
data/output/
data/samples/
data/pilot-sample/

# All MARC binary dumps EXCEPT the intentional test_manuscripts/ fixtures.
data/mrc/*.mrc
!data/mrc/test_manuscripts/

# Secrets / credentials — NEVER commit these
*.token
*token*.txt
*.env
.env.*
*credentials*.json
secrets/
.claude/secrets/

# Audit / revert working files (may contain QID lists or bearer-token cmd lines)
/tmp/wd_token.txt
audit_*.json.bak
revert_*.log


# ─── Added 2026-05-19 ─────────────────────────────────────────────────

# Presentation materials (private to the speaker — slides, speaker notes,
# Q&A notes, generated audio, TTS scripts). Not for the public repo.
docs/presentations/

# Personal supervisor / user guides (work-in-progress documents)
/MHM_Pipeline_User_Guide_he.*
docs/MHM_Pipeline_Supervisor_Guide_he.*
*Supervisor_Guide*
*User_Guide*

# Audio (recordings, TTS output, intermediate uncompressed wavs)
*.wav
*.aiff
*.mp3
*.m4a
*.aac
*.ogg

# Screenshots (personal/draft documentation)
docs/Screenshot*.png

# LaTeX build artifacts at ANY depth (was only paper/ before)
*.aux
*.toc
*.out
*.fls
*.fdb_latexmk
*.xdv
*.synctex.gz
*.nav
*.snm
*.vrb
*.bbl
*.blg

# Generated PDFs at repo root (compiled docs)
/*.pdf

# (Note: paper/ is fully covered by the blanket rule above.)

# Large training data (regenerable from source TSVs; ~900 MB)
processed-data/

# Large authority DB (bundled inside .app; rebuilt by build_mazal_index.py)
converter/authority/mazal_index.db

# IDE / editor local config (machine-specific paths)
.vscode/
.idea/
*.swp
*.swo

# Profile / trace dumps
*.prof
*.trace
